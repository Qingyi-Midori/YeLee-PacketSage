# 项目约定

## 上传限制（重要）

项目完成前，本目录下的任何文件都不得上传到任何远程位置。

具体包括：

- 不执行 `git init` 之外的任何连接远程仓库的操作，不 `git remote add`、不 `git push`
- 不通过 `gh` 命令创建、更新、上传文件到 GitHub 仓库或 Gist
- 不通过任何工具（curl、网页接口、网盘、MCP 等）把本目录文件对外发送
- 需要联网的操作仅限只读查询（例如查看仓库状态），涉及写入或上传的一律先问用户

"项目完成"由用户明确说出口后才算成立；在此之前即使远程仓库 `Qingyi-Midori/YeLee-PacketSage` 是空壳、或用户说"帮我推上去"，也先跟用户确认。

## 敏感文件

`临时deepseekapikey.txt` 等明文密钥文件永远不要提交或上传。
